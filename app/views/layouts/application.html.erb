<!DOCTYPE html>
<html>
  <head>

    <title><%= full_title(yield(:title)) %></title>

    <%= csrf_meta_tags %>
    <%= render 'layouts/shim' %>
    <%= stylesheet_link_tag 'application', media: 'all',
    'data-turbolinks-track' => true %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>

    <script src="http://ajax.googleapis.com/ajax/libs/jquery/1.9.1/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/jquery.superslides/0.6.2/jquery.superslides.js"></script>

    <meta property="fb:app_id" content="fbappid"/>
    <meta property="og:type" content= "website" />
    <meta property="og:image" content= <%= @og_image %> />

    <meta property="og:title" content= "<%= @og_title %>" />
    <meta property="og:description" content= "<%= @og_message %>" />
	<meta name="description" content="Mick Medium takes snap shots of NYC using a pocket camera. A new take on the east river, the manhattan sklyine, and more mundane fanfare that paints the scenery of NYC (mostly east side)">
  
	<meta name="keywords" content="new york city photography, manhattan sklyine, mick medium photography, east river new york city, art deco, tidbits of history">
	<meta name="author" content="Mick Medium">
      <link rel="image_src" href="MAM11bc.png" /><!-- <link rel="stylesheet" href="/assets/stylesheets/superslides.css">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/jquery.superslides/0.6.2/stylesheets/superslides.css">
    <script src="/assets/javascripts/jquery.easing.1.3.js"></script>
    <script src="/assets/javascripts/jquery.animate-enhanced.min.js"></script>
    <script src="/assets/javascripts/jquery.superslides.js" type="text/javascript" charset="utf-8"></script>  -->
	<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</script>
  </head>

  <body>
  		
  	<%= render 'layouts/header' %>
    <%= render 'layouts/slides'%>
    <div class="main_container">
    	<%# @cat_names %>
    	<span style= "color:black;float:right;font-size:8px;line-height:8px">by mick medium</span>
    	<% if session[:category] != 'favorites' %>
    		<%= render 'layouts/paginator'%>
    	<% end %>
        <br>   
        <%= yield %>
		<%# @photos.each_with_index do |photo, index| %>
			<%# "index=#{index} photo id=#{photo.id} photo name=#{photo.title}" %><br>
		<%## end %>
		<% if params[:action] == "show" %>
        	<%= render 'layouts/footer' %>
        <% end %>
          <%= render 'layouts/modal' %>
    </div>
  </body>
</html>
