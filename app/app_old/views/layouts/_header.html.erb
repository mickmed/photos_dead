<header class="navbar navbar-fixed-top">
  <div class="container">
  <%= link_to image_tag('MAM11bc.png', :alt => 'mick medium', height: '46', id: 'logo', align: 'center', margin: '1px'), root_path, id: "logo" %>
    
      <%= link_to "home", root_path %>&nbsp&nbsp&nbsp
      <%= link_to "abc", {:controller => "photos", :action => "index", :abc => 'abc' }%>
      <% @cats.each do |cat| %>
       <span class="cats"></span><%= link_to cat.name, photos_path(category_id: cat.id) %></span>
       <% end %>
   
     
    
    
  </div>
</header>
